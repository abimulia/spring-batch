logging:
  level:
    com:
      abimulia:
        batch: DEBUG
spring:
  sql:
    init:
      mode: always
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/batchdev
    username: myuser
    password: secret
    driver-class-name: com.mysql.cj.jdbc.Driver
